# Utworzenie aplikacji
python manage.py startapp notatnik


# mojastrona/settings.py
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'ogloszenia',
    'notatnik',
]


# notatnik/models.py
from django.db import models


class Note(models.Model):
    title = models.CharField(max_length=100)
    content = models.TextField()

    def __str__(self):
        return self.title


# notatnik/views.py
from django.shortcuts import get_object_or_404, render
from .models import Note


def note_list(request):
    notes = Note.objects.all()

    return render(
        request,
        "notatnik/note_list.html",
        {"notes": notes},
    )


def note_detail(request, note_id):
    note = get_object_or_404(Note, id=note_id)

    return render(
        request,
        "notatnik/note_detail.html",
        {"note": note},
    )


# notatnik/urls.py
from django.urls import path
from . import views

app_name = "notatnik"

urlpatterns = [
    path("notes/", views.note_list, name="note_list"),
    path("note/<int:note_id>/", views.note_detail, name="note_detail"),
]


# mojastrona/urls.py
from django.contrib import admin
from django.urls import include, path

urlpatterns = [
    path("admin/", admin.site.urls),
    path("", include("ogloszenia.urls")),
    path("", include("notatnik.urls")),
]


<!-- notatnik/templates/notatnik/note_list.html -->

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Lista notatek</title>
</head>
<body>

<h1>Lista notatek</h1>

<ul>
    {% for note in notes %}
        <li>
            <a href="/note/{{ note.id }}/">
                {{ note.title }}
            </a>
        </li>
    {% empty %}
        <li>Brak notatek.</li>
    {% endfor %}
</ul>

</body>
</html>


<!-- notatnik/templates/notatnik/note_detail.html -->

<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>{{ note.title }}</title>
</head>
<body>

<h1>{{ note.title }}</h1>

<p>{{ note.content }}</p>

<p>
    <a href="/notes/">← Powrót do listy notatek</a>
</p>

</body>
</html>


# Wygenerowanie i zastosowanie migracji
python manage.py makemigrations notatnik
python manage.py migrate